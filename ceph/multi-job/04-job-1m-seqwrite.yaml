apiVersion: batch/v1
kind: Job
metadata:
  name: 04-ceph-1m-sw-multi-job
spec:
  completions: 6
  parallelism: 6
  template:
    metadata:
      labels:
        app: 04-ceph-1m-sw-multi-job
    spec:
      restartPolicy: Never
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: kubernetes.io/role
                operator: In
                values: ["compute-node"]
      topologySpreadConstraints:
      - maxSkew: 2
        topologyKey: "kubernetes.io/hostname"
        whenUnsatisfiable: DoNotSchedule
        labelSelector:
          matchLabels:
            app: 04-ceph-1m-sw-multi-job  
      containers:
      - name: 04-ceph-1m-sw-multi-job
        image: projectgreenist/fio-project:1.0
        command: ["fio"]
        args: [
          "--name=04-ceph-1m-sw-multi-job",
          "--filename=/data/fiotest-${HOSTNAME}.tmp",
          "--rw=write",
          "--bs=1M",
          "--ioengine=libaio",
          "--direct=1",
          "--size=2G",
          "--numjobs=2",
          "--iodepth=32",
          "--runtime=60s",
          "--time_based",
          "--group_reporting"
        ]
        env:
        - name: HOSTNAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        volumeMounts:
        - name: fio-test-volume
          mountPath: /data
      volumes:
      - name: fio-test-volume
        persistentVolumeClaim:
          claimName: ceph-block-rwo
